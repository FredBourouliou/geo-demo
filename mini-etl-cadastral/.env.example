# PostgreSQL/PostGIS Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=gis
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Target table for shapefile import
TARGET_TABLE=parcelles

# Field to filter commune by name or INSEE code
COMMUNE_FIELD=nom
COMMUNE_VALUE=Quetigny

# Default SRID for geometries (Lambert-93 for France)
DEFAULT_SRID=2154